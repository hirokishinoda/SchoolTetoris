#ifndef   FIELD_HPP
#define   FILED_HPP

#include "objects.hpp"

int is_field_over(t_mino mino);

int is_hit(t_mino mino);

int row_check(t_mino mino);

void row_clear();

void disp_field();

#endif   // DISPLAY_HPP